x# Sat 25 Feb 2023 02:34:49 PM EST pflint
# This can take about half hour...
#
apt-get update && \
apt-get --yes install git && \
apt-get --yes install gh && \
apt-get install --yes python3 \
python3-pyqt5 \
python3-pyqt5.qtwebengine \
python3-pip
#
# SOURCE:https://github.com/cli/cli/blob/trunk/docs/install_linux.mdcc1
type -p curl >/dev/null || sudo apt install curl -y
curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg \
&& sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg \
&& echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
&& sudo apt update \
&& sudo apt install gh -y
#
python3 setup.py install
python3 -m enki

mkdir -p /home/flint
cd /home/flint
git clone https://github.com/flintiii/bin.git && \
/home/flint/bin/finagrade.sh sane 
&& \


# break here
basename


2>/dev/null <type -p curl >/dev/null || sudo apt install curl -y
curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg \
&& sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg \
&& echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
&& sudo apt update \
&& sudo apt install gh -y
< cut

type 11 and 5

scp flint@192.168.1.242:~flint/.ssh/id_rsa* ~flint/.ssh/. 
gh auth login --hostname flintiii --with-token "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"

gh auth login --hostname flintiii
basename
    dirname(1), readlink(1)

cd ~; rm -rf bin;git clone https://github.com/flintiii/bin.git

cd ~; rm -rf bin; git clone https://github.com/flintiii/bin.git
ls -alt | grep ^l

ls -alt | grep ^l


cp /home/flint/git/bin/flig.sh ./flig
chmod +x flig

apt-get update;
apt-get install -192.168.1.242y python3 \
python3-pyqt5 \
python3-pyqt5.qtwebengine\ /usr/local/lib/python3.10/dist-packages/
python3-pip


pip install Python-Markdown     #\. For Markdown preview
pip install Docutils            #\. For reStructuredText preview
pip install ctags               #\. For navigation in file
pip install regex               #\. For preview synchronization
pip install CodeChat            #\. For source code to HTML translation (literate programming)
pip install Sphinx              #\. To build Sphinx documentation.
pip install Flake8              #\. To lint your Python code.


gh auth login --hostname flintiii --with-token "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"
git clone git@github.com:andreikop/qutepart.git
cd qutepart
python3 setup.py install
python3 -m enki


adduser flint
chown flint:flint /home/flint

apt-get install sudo vim

python3-pyqt5.qtwebengine \or
python3-qt5-webengine
 
# sudo apt-get install python3 libqt5svg5 python3-pyqt5 python3-pyqt5.qtwebengine python3-markdown python3-docutils ctags
   pip3 install -r requirements.txt


clisp-module-pcre - GNU CLISP module that adds libpcre support

apt-get install clisp-module-pcre

root@3fda94ca4dc1:/home/flint/qutepart# apt-get install gcc

apt-get install python3 libqt5svg5 python3-pyqt5 python3-pyqt5.qtwebengine python3-markdown python3-docutils universal-ctags
***

SOURCE: https://github.com/cli/cli/blob/trunk/docs/install_linux.md
type -p curl >/dev/null || sudo apt install curl -y
curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg \
&& sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg \
&& echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
&& sudo apt update \
&& sudo apt install gh -y

cut
